{% extends 'MaallsAdminBundle::head.html.twig' %}
{% block body %}
<body>
    <div id="wrapper">
        <nav class="navbar-default navbar-static-side" role="navigation">
            {% block sideNavContainer %}
                {% block sideNav %}
                {% endblock %}
            {% endblock %}
        </nav>
        <div id="page-wrapper" class="gray-bg dashbard-1">
            <div class="row border-bottom">
                <nav class="navbar navbar-static-top" role="navigation" style="margin-bottom: 0">
                    <div class="navbar-header">
                        <a class="navbar-minimalize minimalize-styl-2 btn btn-primary " href="javascript:"><i class="fa fa-bars"></i> </a>                        
                    </div>
                    <ul class="nav navbar-top-links navbar-right">
                        {% block topRightNav %}{% endblock %}
                        <li>
                            <a href="{{ path('fos_user_security_logout') }}">
                                <i class="fa fa-sign-out"></i> Log out
                            </a>
                        </li>
                    </ul>
                </nav>
                {% for type in ['success', 'notice', 'error', 'danger', 'warning', 'info'] %}
                    {% for message in app.session.flashbag.get(type) %}
                        <div class="alert alert-{{type}} alert-dismissable text-center">
                            <button aria-hidden="true" data-dismiss="alert" class="close" type="button">×</button>
                            {{ message | raw }}
                        </div>
                    {% endfor %}
                {% endfor %}
            </div>
            {% block mainContainer %}
                {% block main %}
                {% endblock %}
            {% endblock %}
        </div>
    </div>
    <!-- Mainly scripts -->
    {% include 'MaallsAdminBundle::javascripts.html.twig' %}
    {% block javascripts %}
   
    {% endblock %}
    
</body>

{% endblock %}